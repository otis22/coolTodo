# Task E11: Создать скрипт инициализации проекта

**Статус**: Completed ✅  
**Начало**: 2025-01-27  
**Завершение**: 2025-12-01  
**Приоритет**: Medium  
**Оценка**: 0.5 дня

## Описание

Создать скрипт для автоматической инициализации проекта при первом запуске. Скрипт должен выполнять все необходимые шаги для настройки локального окружения разработки.

## Проблема

**Текущая ситуация**:
- Разработчику нужно вручную выполнять множество команд для инициализации проекта
- Легко забыть какой-то шаг (создание .env, генерация ключа, миграции)
- Нет единой точки входа для первоначальной настройки

**Требуемые шаги для инициализации**:
1. Создать `.env` из `.env.example`
2. Сгенерировать `APP_KEY`
3. Запустить Docker контейнеры
4. Установить зависимости backend (composer)
5. Выполнить миграции
6. Установить зависимости frontend (npm)
7. Проверить, что все работает

## Зависимости

- [x] E9: Настроить локальное окружение (.env) (Completed ✅)

## Требования

### Цель

Создать скрипт `./init` или `./setup` для автоматической инициализации проекта.

### Критерии приемки

✅ Скрипт создает `.env` из `.env.example` (если не существует)  
✅ Скрипт генерирует `APP_KEY` (если не установлен)  
✅ Скрипт запускает Docker контейнеры  
✅ Скрипт устанавливает зависимости backend  
✅ Скрипт выполняет миграции  
✅ Скрипт устанавливает зависимости frontend  
✅ Скрипт проверяет работоспособность (опционально)  
✅ Скрипт идемпотентен (можно запускать несколько раз без ошибок)  
✅ Скрипт документирован в README

## Системное решение

### Вариант 1: Bash скрипт `./init` (Рекомендуется)

**Преимущества**:
- Простота реализации
- Работает на всех Unix-системах
- Легко расширять

**Реализация**:
- Создать `init` скрипт в корне проекта
- Использовать `./dev` для выполнения команд в контейнерах
- Проверять наличие файлов перед созданием
- Выводить понятные сообщения о прогрессе

### Вариант 2: Makefile

**Недостатки**: Требует установки `make`, менее гибкий

### Выбранный вариант: Вариант 1 (Bash скрипт)

## План выполнения

### Шаг 1: Создание скрипта
- [x] Создать файл `init` в корне проекта ✅
- [x] Добавить shebang `#!/bin/bash` ✅
- [x] Добавить проверку наличия Docker и docker-compose ✅
- [x] Реализовать проверку существования `.env` ✅
- [x] Реализовать создание `.env` из `.env.example` ✅

### Шаг 2: Генерация ключа и настройка Laravel
- [x] Проверить наличие `APP_KEY` в `.env` ✅
- [x] Выполнить `./dev artisan key:generate` если ключ отсутствует ✅
- [x] Очистить кэш конфигурации ✅

### Шаг 3: Запуск Docker и установка зависимостей
- [x] Запустить `docker-compose up -d` ✅
- [x] Дождаться готовности MySQL ✅
- [x] Выполнить `./dev composer install` ✅
- [x] Выполнить `./dev artisan migrate` ✅

### Шаг 4: Установка зависимостей frontend
- [x] Выполнить `cd frontend && npm ci` ✅
- [x] Вернуться в корневую директорию ✅

### Шаг 5: Проверка работоспособности (опционально)
- [x] Проверить доступность API (`curl http://localhost:8080/api/todos`) ✅
- [x] Вывести информацию о следующих шагах ✅

### Шаг 6: Документация
- [x] Обновить README с инструкцией по использованию скрипта ✅
- [x] Добавить примеры использования ✅
- [ ] Упомянуть в DEVELOPMENT.md (опционально)

### Шаг 7: Тестирование
- [x] Протестировать на чистом репозитории ✅
- [x] Протестировать идемпотентность (повторный запуск) ✅
- [x] Проверить обработку ошибок ✅

## Выполненные работы

### Создан скрипт `./init`

Скрипт автоматически выполняет все необходимые шаги для инициализации проекта:

1. ✅ Проверяет наличие необходимых инструментов (Docker, Docker Compose, npm)
2. ✅ Создает `.env` из `.env.example` (если не существует)
3. ✅ Генерирует `APP_KEY` (если не установлен)
4. ✅ Запускает Docker контейнеры
5. ✅ Ожидает готовности MySQL
6. ✅ Устанавливает зависимости backend (composer install)
7. ✅ Выполняет миграции БД
8. ✅ Устанавливает зависимости frontend (npm ci)
9. ✅ Проверяет работоспособность API (опционально)
10. ✅ Выводит информацию о следующих шагах

### Особенности реализации

- **Идемпотентность**: Скрипт можно запускать несколько раз без ошибок
- **Цветной вывод**: Используются цвета для лучшей читаемости
- **Обработка ошибок**: Скрипт останавливается при критических ошибках
- **Информативные сообщения**: Каждый шаг сопровождается понятным сообщением
- **Проверка требований**: Проверяется наличие всех необходимых инструментов

### Обновлена документация

- ✅ README.md обновлен с инструкцией по использованию скрипта `./init`
- ✅ Добавлен альтернативный способ ручной настройки

## Пример использования

```bash
# Первая инициализация проекта
./init

# Скрипт выполнит все необходимые шаги и выведет:
# ✅ Environment configured
# ✅ Application key generated
# ✅ Docker containers started
# ✅ Backend dependencies installed
# ✅ Database migrated
# ✅ Frontend dependencies installed
# 
# 🎉 Project initialized successfully!
# 
# Backend API: http://localhost:8080/api
# Frontend: http://localhost:5173
```

## Прогресс

Создано: 2025-01-27

