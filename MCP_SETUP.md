# Инструкция по настройке MCP серверов

## ✅ Выполнено

1. ✅ Установлен Node.js 20.19.6 через nvm
2. ✅ Создан конфигурационный файл MCP: `~/.cursor/mcp.json`
3. ✅ Настроен Context7 MCP сервер

## 📋 Что нужно сделать

### 1. Получить API ключ для Context7

1. Перейдите на [context7.com](https://context7.com/)
2. Зарегистрируйтесь или войдите в систему
3. Перейдите в [Dashboard](https://context7.com/dashboard)
4. Скопируйте ваш API ключ

### 2. Обновить конфигурацию MCP

Откройте файл `~/.cursor/mcp.json` и замените `ВАШ_API_КЛЮЧ_ЗДЕСЬ` на ваш реальный API ключ:

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": [
        "-y",
        "@upstash/context7-mcp",
        "--api-key",
        "ВАШ_РЕАЛЬНЫЙ_API_КЛЮЧ"
      ]
    }
  }
}
```

### 3. Перезапустить Cursor

После обновления конфигурации:
1. Закройте Cursor полностью
2. Откройте Cursor снова
3. MCP серверы должны автоматически подключиться

## 🌐 Browser MCP

Для работы с браузером через MCP установите расширение для Chrome:
- [Веб-MCP: Служба MCP для браузера](https://chromewebstore.google.com/detail/веб-mcp-служба-mcp-для-бра/acdlpjcmkabbmhpibedepbfdankiagoc?hl=ru)

После установки расширения вы сможете использовать браузерные функции через MCP.

## 🔍 Проверка работы

После перезапуска Cursor вы должны увидеть MCP серверы в списке доступных ресурсов. Проверьте через команду в Cursor или через интерфейс MCP.

## 📝 Примечания

- Node.js установлен через nvm в `~/.nvm/`
- nvm автоматически загружается при открытии нового терминала
- Если Cursor не видит npx, может потребоваться добавить путь к npx в конфигурацию или использовать полный путь

